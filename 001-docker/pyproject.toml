[project]
name = "pipeline"
version = "0.0.1"
dependencies = [
    "pandas==2.3",
    "pyarrow==22.0.0"
]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
