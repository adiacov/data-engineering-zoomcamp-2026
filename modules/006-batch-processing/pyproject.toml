[project]
name = "batch"
version = "0.1.0"
description = "Data Engineering Zoomcamp — Module 6: Batch Processing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=4.1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
