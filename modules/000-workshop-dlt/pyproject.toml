[project]
name = "workshop_dlt"
version = "0.1.0"
description = "Data Engineering Zoomcamp — Workshops DLT"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["dlt[duckdb]>=1.22.1"]

[project.scripts]
workshop_dlt = "workshop_dlt.pipeline:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
