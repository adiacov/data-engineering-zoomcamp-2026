[project]
name = "platforms"
version = "0.1.0"
description = "Data Engineering Zoomcamp — Module 05 Data Platforms"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
